<%= form_for [topic, post, comment] do |f| %>
     <%= f.label :comment %>
     <%= f.text_field :body, class: 'form-control', placeholder: "Enter comment text" %>
   <div class="form-group">
    <%= f.submit "Post", class: 'btn btn-success' %>
   </div>
 <% end %>